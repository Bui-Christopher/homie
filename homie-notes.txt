TODO

.github/workflows/.lint.yaml
    - branches
        - Run linters on other branches other than 'main'

    - concurrency
        - What is cancel-in-progress?

    - jobs
        - strategy
            - What is fail-fast?
        - matrix
            - Comment out apple-darwin
            - Remove commented code
        - steps
            - Should this version be used? 'uses: actions-rust-lang/setup-rust-toolchain@v1.8.0'

    - Get linter to work
        - Currently failing because certain bins require nightly
.sqlx
    - Wut dis?
docker
    - The dockerfiles/build.sh look similar: Refactor into one
        - Pass the bin name instead
    - Remove .darwin
homie-api
    - init_tracing before init_config
    - Why is this done in setting up logging tracer?
        - .add_directive("sqlx::query=warn".parse()?), // Silence SQLx calls
homie-core
    - adapter
        - config
            - Add a space on line #38 for readability on new config
            - Why are the dataset_config cloned?
                - Oh... Config is &'static, thus we clone it to pass into importer
                - What is &' instead of &static?
        - repository
            - mod
                - Fix is_zillow_api_enabled
                    - Exclusively reads by HTTP calls
                    - Should be both postgres + zillow API
            - database/postgres
                - Ew, dis ugly
homie-webapp
    - It empty 😭😓😞
test (csr-plot)
    - Remove bins

.toml files
    - review Makefile.toml
    - Check dependency versions
